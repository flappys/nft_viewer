export default function profilerCallback(
  id,
  phase,
  actualDuration,
  baseDuration,
  startTime,
  commitTime,
  interactions
) {
  // console.log(`${id} (${phase}) Actual Duration: ${actualDuration}`);
}
